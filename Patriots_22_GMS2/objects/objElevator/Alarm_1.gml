/// @description trigger "closed" flag
doneClose = 1;
doneOpen = 0;
mask_index = sprElevatorMaskClosed;
//disable teleport if the player is outside of the elevator
//check to make sure player isn't inside or too close to the elevator before changing the mask!